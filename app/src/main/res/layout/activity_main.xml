<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    tools:context="com.deepthink.entranceguard.MainActivity">

    <com.deepthink.entranceguard.view.FaceDetectView
        android:id="@+id/detect_view"
        android:layout_width="match_parent"
        android:layout_height="match_parent" />

    <ImageView
        android:id="@+id/register"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_alignParentBottom="true"
        android:layout_centerHorizontal="true"
        android:layout_marginBottom="23dp"
        android:src="@android:drawable/ic_menu_camera" />

    <ImageView
        android:id="@+id/border"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:src="@drawable/border"
        android:visibility="invisible" />

    <ImageView
        android:id="@+id/matched"
        android:layout_width="150dp"
        android:layout_height="150dp"
        android:layout_alignParentRight="true"
        android:layout_alignParentTop="true"
        android:layout_marginRight="15dp"
        android:layout_marginTop="15dp"
        android:src="@drawable/nobody"
        android:visibility="invisible" />

    <TextView
        android:id="@+id/info"
        android:layout_width="150dp"
        android:layout_height="50dp"
        android:layout_alignStart="@+id/matched"
        android:layout_below="@+id/matched"
        android:layout_marginTop="14dp"
        android:text="男 15岁"
        android:textAlignment="center"
        android:textSize="18sp" />

</RelativeLayout>
